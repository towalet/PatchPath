# ---------------------------------------------------------------------------
# PatchPath backend dependencies (pinned for reproducible builds).
# ---------------------------------------------------------------------------

# Core framework
Django==5.0.6
djangorestframework==3.15.1
djangorestframework-simplejwt==5.3.1

# API plumbing
django-cors-headers==4.4.0
django-environ==0.11.2

# Database driver
psycopg[binary]==3.2.1

# Production serving / static
gunicorn==22.0.0
whitenoise==6.7.0

# AI + structured output
openai==1.40.0
httpx==0.27.2
pydantic==2.8.2

# Test + dev tooling (kept here for a single-stage MVP image; split into
# requirements-dev.txt if you want leaner production images later).
pytest==8.3.2
pytest-django==4.8.0
pytest-cov==5.0.0
factory-boy==3.3.0
ruff==0.5.6
