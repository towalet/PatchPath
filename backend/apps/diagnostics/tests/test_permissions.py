"""Ownership/permission tests: users cannot reach each other's records.

Scaffold placeholder — see docs/AGENT_PLAN.md §9 + §17.
"""
import pytest

pytestmark = pytest.mark.django_db


@pytest.mark.skip(reason="scaffold: implement ownership-scoping tests")
def test_placeholder():
    ...
