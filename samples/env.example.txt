# Example .env for the demo app. Note DATABASE_URL is documented here but was
# never set in the Render dashboard — that mismatch is the root cause.
SECRET_KEY=replace-with-a-long-random-value
DEBUG=False
ALLOWED_HOSTS=django-render-api.onrender.com

# Required in production but missing from the Render service environment:
# DATABASE_URL=postgres://user:password@host:5432/dbname
