# Frontend environment. Copy to `.env` (or `.env.local`) for local dev.
# Vite only exposes variables prefixed with VITE_.

# Base URL of the PatchPath API (browser -> backend, via host port mapping).
VITE_API_BASE_URL=http://localhost:8000/api
