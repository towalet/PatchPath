/** Top navigation bar: brand, user menu, logout. Scaffold stub. */
export function TopNav() {
  return (
    <header data-component="top-nav">
      <span>PatchPath</span>
      {/* TODO: user menu + logout */}
    </header>
  );
}
