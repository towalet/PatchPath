/**
 * Copyable list of commands to run, rendered in mono. Warn on anything
 * destructive (none auto-applied). Scaffold stub.
 */
export function CommandList(_props: { commands: string[] }) {
  return <ul data-component="command-list">{/* TODO: render commands */}</ul>;
}
