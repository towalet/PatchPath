/**
 * Missing information — always present in every report so uncertainty stays
 * visible (see AGENT_PLAN §12). Scaffold stub.
 */
export function MissingInformation(_props: { items: string[] }) {
  return <ul data-component="missing-information">{/* TODO: render gaps */}</ul>;
}
