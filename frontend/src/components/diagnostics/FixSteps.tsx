/** Recommended fix narrative. Careful, non-hype language. Scaffold stub. */
export function FixSteps(_props: { recommendedFix: string }) {
  return <div data-component="fix-steps">{/* TODO: render recommended fix */}</div>;
}
