/** Login form. Stores tokens, redirects to dashboard. Scaffold stub. */
export default function LoginPage() {
  return (
    <main data-route="login">
      <h1>Log in</h1>
      {/* TODO: login form via useAuth().login */}
    </main>
  );
}
