/** Registration form. Scaffold stub. */
export default function RegisterPage() {
  return (
    <main data-route="register">
      <h1>Create your account</h1>
      {/* TODO: register form via useAuth().register */}
    </main>
  );
}
