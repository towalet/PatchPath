/**
 * New analysis flow: session summary, file dropzone, pasted error box, upload
 * validation, analyze button (enabled once evidence exists), timeline. Scaffold stub.
 */
export default function NewAnalysisPage() {
  return (
    <main data-route="new-analysis">
      <h1>New analysis</h1>
      {/* TODO: upload + paste + analyze workflow */}
    </main>
  );
}
