/** Session history with project/status/severity filters. Scaffold stub. */
export default function SessionHistoryPage() {
  return (
    <main data-route="history">
      <h1>History</h1>
      {/* TODO: filters + session/report summaries */}
    </main>
  );
}
