/**
 * Dashboard: project/session/report summaries + empty state to create the first
 * project. Fetch dashboard data with Promise.all. Scaffold stub.
 */
export default function DashboardPage() {
  return (
    <main data-route="dashboard">
      <h1>Dashboard</h1>
      {/* TODO: summary cards + recent sessions */}
    </main>
  );
}
